meta {
  name: Login
  type: http
  seq: 13
}

post {
  url: http://localhost:8080/api/auth/
  body: json
  auth: inherit
}

body:json {
  {
      "username": "admin",
      "password": "admin123"
  }
}

settings {
  encodeUrl: true
}
